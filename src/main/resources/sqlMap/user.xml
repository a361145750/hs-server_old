<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="userMapper">

    <select id="queryUserByLoginId" parameterType="java.util.Map" resultType="java.util.Map">
        select *
        from s_user
        where login_id = #{loginId,jdbcType=VARCHAR}
    </select>

    <select id="queryUser" parameterType="java.util.Map" resultType="java.util.Map">
        select *
        from s_user
        where 1=1
        <if test="loginId!=null and loginId!=''">and login_id like concat('%',#{loginId,jdbcType=VARCHAR},'%' )</if>
    </select>

    <insert id="insertUser" parameterType="java.util.Map">
        insert into s_user
        (`login_id`, `user_name`, `pass_word`, `role`, `user_right`, `creat_by`, `creat_date`)
        values(#{loginId,jdbcType=VARCHAR},#{userName,jdbcType=VARCHAR},#{passWord,jdbcType=VARCHAR},#{role,jdbcType=BIGINT},#{userRight,jdbcType=BIGINT},#{oprateUserId,jdbcType=VARCHAR},now())
    </insert>

    <update id="updateUser" parameterType="java.util.Map">
        update s_user set login_id=#{loginId,jdbcType=VARCHAR}, user_name=#{userName,jdbcType=VARCHAR}, pass_word=#{passWord,jdbcType=VARCHAR}, role=#{role,jdbcType=BIGINT}, user_right=#{userRight,jdbcType=BIGINT}, update_by=#{oprateUserId,jdbcType=VARCHAR}, update_date=now()
        where user_id = #{userId,jdbcType=VARCHAR}
    </update>

    <delete id="deleteUser" parameterType="java.util.Map">
        delete from s_user
        where user_id = #{userId,jdbcType=VARCHAR}
    </delete>

</mapper>